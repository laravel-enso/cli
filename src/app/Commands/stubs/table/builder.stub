<?php

namespace App\Tables\Builders${namespaceSegment};

use App\${Model};
use LaravelEnso\VueDatatable\app\Classes\Table;

class ${Model}Table extends Table
{
    protected $templatePath = __DIR__.'${depth}../Templates/${pathSegment}/${models}.json';

    public function query()
    {
        return ${Model}::select(\DB::raw('${models}.id as "dtRowId" '));
    }
}
