const ${Model}Show = () => import('../../../pages/${segment}/${models}/Show.vue');

export default {
    name: '${segment}.${models}.show',
    path: ':id',
    component: ${Model}Show,
    meta: {
        breadcrumb: 'show',
        title: '${Model} Profile',
    },
};
