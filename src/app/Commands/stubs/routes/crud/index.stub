const ${Model}Index = () => import('../../../pages/${segment}/${models}/Index.vue');

export default {
    name: '${segment}.${models}.index',
    path: '',
    component: ${Models}Index,
    meta: {
        breadcrumb: 'index',
        title: '${Model} Index',
    },
};
