const ${Model}Index = () => import('${depth}../pages/${pathSegment}/Index.vue');

export default {
    name: '${nameSegment}.index',
    path: '',
    component: ${Models}Index,
    meta: {
        breadcrumb: 'index',
        title: '${Model} Index',
    },
};
